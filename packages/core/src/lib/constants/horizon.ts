export const GRID_SIZE_COARSE = 20
export const GRID_SIZE_FINE = 10

// "Top edge" quick snap threshold (y <= QUICK_SNAP_THRESHOLD)
export const QUICK_SNAP_THRESHOLD = 18

// Multiplied with the grid size to determine the snap factor
export const EDGE_SNAP_FACTOR = 1

// "Styling"
// Safe area around app edges which are not to be used for content
// These are also draggable for moving the app window
export const SAFE_AREA_PADDING = 10

export const APP_BAR_WIDTH = 70
