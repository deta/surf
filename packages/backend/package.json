{
  "name": "@horizon/backend",
  "version": "0.1.0",
  "description": "",
  "files": [
    "index.node"
  ],
  "main": "index.node",
  "scripts": {
    "dev": "npm run build",
    "build-cmd": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "build-debug": "npm run build-cmd --",
    "build": "npm run build-cmd -- --release",
    "install": "npm run build",
    "test": "cargo test -- --nocapture"
  },
  "author": "",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
